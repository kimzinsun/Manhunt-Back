<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tovelop.maphant.mapper.UserMapper">
    <select id="readUploadLog" resultType="UploadLogDTO">
        SELECT *
        FROM UploadLog
    </select>
    <insert id="insertUploadLog">
        INSERT INTO UploadLog (number, user_id, file_size, upload_date, url)
        VALUES (#{number}, #{user_id}, #{file_size}, #{upload_date}, #{url})
    </insert>
    <select id="readUploadLogByUserId" resultType="UploadLogDTO">
        SELECT *
        FROM UploadLog
        WHERE user_id = #{user_id}
    </select>
</mapper>